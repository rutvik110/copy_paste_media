## 0.1.0

- Initial pub.dev release.
- Copy images to the macOS clipboard with `copyImage`.
- Paste images from the macOS clipboard with `pasteImage`.

## 0.0.2

- Rename the package from `copyimageflutter` to `copy_paste_media`.
- Add `pasteImage()` to read an image from the macOS clipboard.

## 0.0.1

- Copy images to the macOS clipboard.
